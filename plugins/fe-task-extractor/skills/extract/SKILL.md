---
name: fe-task-extractor
description: 기획서나 요구사항 문서에서 프론트엔드 작업만 추출하여 세분화된 작업 목록을 마크다운으로 생성하는 스킬. 사용자가 기능 요구사항, PRD, 기획서, 스펙 문서 등을 제공하고 "프론트엔드 작업 추출", "FE 태스크 분리", "프론트 작업 정리", "작업 세분화", "프론트엔드 할 일 뽑아줘" 등을 요청할 때 반드시 이 스킬을 사용한다. 문서에 백엔드, 디자인, 기획이 섞여 있어도 프론트엔드 영역만 정확히 식별해서 뽑아내야 할 때 이 스킬이 필요하다. 텍스트 직접 입력, docx/pdf 파일 업로드 등 다양한 입력 형태를 지원한다. 노션/컨플루언스 링크가 제공되면 텍스트 복붙을 유도한다.
---

# Frontend Task Extractor

기획서·PRD·요구사항 문서에서 **프론트엔드 작업만 식별·추출**하여 **요구사항 명세**를 생성한다. 구현 계획·AC는 다루지 않는다 — plan mode에서 처리.

출력:
1. 마크다운 파일 → `{프로젝트 루트}/.docs/task/`
2. Jira Story → Atlassian MCP

---

## 1. 입력 처리

**텍스트** → 그대로 분석. **파일** → 파일 읽기 스킬로 추출 후 분석. **노션/컨플루언스 링크** → web_fetch 하지 않고 텍스트 복붙 유도:
> "페이지 내용을 텍스트로 복사해서 붙여넣어 주세요."

---

## 2. 분석

### Step 1: 전체 이해
기능 목적, 사용자 시나리오, 시스템 구성 파악. 아직 작업을 뽑지 않는다.

### Step 2: 영역 분류 → FE만 추출
- **FE**: UI, 인터랙션, 상태 관리, 클라이언트 API 호출, 라우팅, 폼, 유효성 검사(클라이언트), 반응형/접근성, 에러 UI, 로딩/스켈레톤
- **BE**: API 구현, DB, 비즈니스 로직, 서버 인증/인가
- **디자인**: 와이어프레임, 비주얼, 에셋
- **기획/기타**: 정책, QA, DevOps

### Step 3: 세분화
복잡도에 따라 유동적: 단순 1개 / 중간 2~5개 / 복잡 5~10+. 서로 다른 페이지·컴포넌트 → 분리. 독립 관심사 → 분리 후보. 함께 구현되는 것 → 묶기.

### Step 4: 의존관계·API 매핑
각 작업에 선행/후행 + API 연동 정보 기록.

---

## 3. 출력

### 3-1. 마크다운 저장

파일 경로 생성은 스크립트에 위임한다:

```bash
python3 scripts/init_task_dir.py "{기능명}" [--root {경로}]
```

반환 JSON에서 `path`를 사용하여 마크다운을 저장한다. 프로젝트 루트를 자동 탐색하며, 못 찾으면 에러를 반환하므로 사용자에게 경로를 물어본다.

**마크다운 포맷** — 요구사항 명세만 담는다. 구현 계획·AC·체크리스트 금지:

```markdown
# FE 작업: {기능명}

> 원본 요구사항에서 프론트엔드 영역만 추출한 요구사항 명세.

- 총 작업: {N}개
- 출처: {입력 형태}
- jira: 미생성

---

## FE-01. {작업 제목}
{무엇을·왜 1~2문장. 핵심 동작을 → 화살표로.}

- deps: ←{선행} →{후행}
- api: `{METHOD} {endpoint}` {설명}
- states: {상태 흐름}
- jira: 미생성
- note: {[확인 필요: ...] 필요시만}

---
```

**필드 규칙**:
- `deps`: `←` 선행, `→` 후행. 없으면 생략 또는 `없음`
- `api`: 백틱 `METHOD /path` + body 요약. 없으면 `없음`
- `states`: `→` 순차, `·` 분기. 단순하면 생략
- `note`: 원문 불명확시만. 기본 생략
- 구현 방법, AC, 체크리스트는 **적지 않는다**

### 3-2. Jira Story 생성

마크다운 저장 후 Jira에 Story 등록.

**사전 조건**: Atlassian MCP 연결 확인 (불가 시 마크다운만 저장 후 안내) → 프로젝트 키 확인 → 에픽 연결 여부 확인.

**`jiraCreateIssue` 호출** — 타입: Story, summary: 작업 제목, description:
```
{설명 1~2문장}

- 선행: {작업 제목 또는 "없음"}
- 후행: {작업 제목 또는 "없음"}
- API: {엔드포인트 또는 "없음"}
- UI 상태: {states 또는 "N/A"}
```

**생성 후** — 이슈 키 반영을 스크립트에 위임:
```bash
python3 scripts/update_jira_keys.py {md파일경로} '{"FE-01":"PROJ-101",...}' --project {프로젝트키}
```

이후 사용자에게 결과 요약 보고.

---

## 4. 주의사항

- **FE 경계**: 클라이언트 유효성 검사·에러 UI·로딩은 FE. SSR 서버 로직·API 핸들러 비즈니스 로직은 BE.
- **원문에 없는 작업 추가 금지**. 암시된 것은 "추가 고려 사항"에 제안.
- **BE 작업 미추출** — `api:` 필드에 참조만.
- **기술 스택 중립** — 사용자 언급 시 용어 조정.
- **원문 불명확** → `note: [확인 필요: ...]`.
- **Jira 실패** → `jira: 생성 실패 - {사유}` 기록, 나머지 계속.
- **이 스킬은 "무엇을"만** — "어떻게"는 plan mode.

---

## 5. 추가 고려 사항 (선택적)

원문 미포함이지만 FE 구현 시 검토 필요한 항목. Jira 미생성. 원문이 충분하면 생략.

---

## 6. scripts/

| 스크립트 | 용도 | 호출 시점 |
|---------|------|----------|
| `init_task_dir.py` | 프로젝트 루트 탐색, `.docs/task/` 생성, 파일 경로 반환 | 마크다운 저장 직전 |
| `update_jira_keys.py` | Jira 이슈 키를 마크다운에 일괄 반영 | Jira 티켓 생성 완료 후 |
