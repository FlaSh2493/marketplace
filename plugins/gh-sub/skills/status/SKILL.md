---
name: gh-sub-status
description: Show the current repository's git configuration and check if it matches a known profile.
---

I will check the current repository's:
- `user.name`
- `user.email`
- `remote.origin.url`

And compare them against the profiles stored in `~/.gh-sub/profiles.json` to tell you which account is currently active.
