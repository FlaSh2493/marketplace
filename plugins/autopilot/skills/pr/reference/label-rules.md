# 라벨 추론 규칙

## 자동 추론 로직
- `infer_labels.py` 스크립트가 다음 로직에 따라 추론함:
  1. `base_branch` (예: `develop`, `main`)
  2. 변경 파일 경로의 최상위 의미 있는 디렉토리명 (도메인 라벨)

## 검증 규칙
- **교차 검증**: 추론된 모든 라벨은 `gh label list` 결과에 존재하는 것만 반환함.
- **중복 제거**: 중복된 라벨은 하나로 통합함.
- **개수 제한**: 노이즈 방지를 위해 도메인 라벨은 최대 2개까지만 포함함.
